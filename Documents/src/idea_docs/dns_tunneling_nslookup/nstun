challenges:
	two messages are gonna end up the same
	and the results is cached therefore not sent
solutions:
	wait for cache to clear
	assume the message was the same

every message consists of:
	client id


handshake:
	first the client must introduce themselves
	client sends the client id in the first question and awaits ack

the communication:
	the next client id is : sha256sum(currentid.appent(currentid % 256))
	a message is like this:
		currentid-message-next(currentid)
	two messages are like this
		currentid-message-next(currentid)-message-next(next(currentid))	

	the server needs to calculate each "currentid" to be able to know where is the currentid and for which client is
	 

the communication channel is the lookup method for the client ( which may be the getaddrinfo(3) )


